<% if @steps.any? %>
	<ul>
	<% @steps.each do |f| %>
		<li>
			<%= f.description %>

			<% if f.filepicker_url? %>
				<%= filepicker_image_tag f.filepicker_url, w: 60, h: 60 %>
			<% end %>
		</li>
	<% end %>
	</ul>
<% end %>