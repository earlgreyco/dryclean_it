<% if @saved %>
	$("#new-step-form")[0].reset();
	$(".step-list").append("<%= j render(@step) %>");
	$("#recipe-id-field-two").val("<%= @recipe.id %>");
	$('.remove-step').show();
	$(".remove-step").click(function(){
		$(this).parent().remove();
	});
<% end %>