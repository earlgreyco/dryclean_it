<% if @saved %>
	$(".recipe-images").append("<%= j render @recipe_image %>");
	$("#recipe-id-field-three").val("<%= @recipe.id %>");
	$(".remove-recipe-image").show();
	$(".remove-recipe-image").click(function(){
    $(this).parent().html("No image");
  });
<% end %>