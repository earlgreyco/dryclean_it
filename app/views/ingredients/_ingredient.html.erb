<tr>
	<td>
		<%= ingredient.name %>
	</td>

	<td>
		<%= ingredient.definition %>
	</td>

	<td>
		<%= ingredient.link %>
	</td>

	<td>
		<%= link_to "edit", edit_ingredient_path(ingredient) %> /
		<%= link_to "delete", ingredient, method: :delete,
																	data: { confirm: "Sure you want to delete #{ingredient.title}?" } %>
	</td>
</tr>