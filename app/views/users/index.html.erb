<% provide(:title, 'All users') %>
<h1>All users</h1>

<% if @users.length > 0 %>

	<%= will_paginate %>

	<table>
		<tbody>
			<%= render @users %>
		</tbody>
	</table>

	<%= will_paginate %>

<% end %>